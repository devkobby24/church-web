/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['https://gloriousassuranceministires.vercel.app'], // Replace with your Vercel domain, e.g., 'your-project-name.vercel.app'
    },
  };
  
  module.exports = nextConfig;
  